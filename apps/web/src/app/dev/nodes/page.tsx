// Dev-only node gallery — visual reference for the six structural-type
// node components. Useful for iterating on visual treatment without
// loading a real flow.

import { NodeGallery } from '@/components/canvas/dev/NodeGallery';

export default function NodesGalleryPage() {
  return <NodeGallery />;
}
